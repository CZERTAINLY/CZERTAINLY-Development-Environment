pool pool.ntp.org iburst

driftfile /var/lib/chrony/drift

makestep 1.0 3

# Allow any client to use this server as an NTP source
allow all

# Serve time even when not synced, as a fallback
local stratum 10
